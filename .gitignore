.gitattributes
.dump
.data
*.so
*.o
*.a

doc/manual/manual.html

test/time.txt
test/time-debug.txt

test/libs/all

temp
lua
dist
.vscode
.xtrpatch
.xtrshow
x.md
x.patch
.nogit

luac.out
release_dist
.xtrshow_manifest

# Python bytecode from script/
__pycache__/
*.pyc

# Workflow agents get their own worktrees under here; they are scratch space.
.claude/worktrees/

# Built by examples/discofetch/Makefile
examples/discofetch/swarmd

# The landing page (site/): build output, the smoke test's dependencies and
# its screenshots. See site/README.md.
site/_out/
site/node_modules/
site/test/.out/

# Built browser wasm (produced by `make build_browser`)
web/diluvium_browser.wasm
web/*.wasm

# The docs site (docsite/): node_modules, the Docusaurus build output and
# its cache. build/ is what deploy/cloud1/docs/deploy.sh ships; none of it
# is committed. See docsite/README.md.
docsite/node_modules/
docsite/build/
docsite/.docusaurus/
